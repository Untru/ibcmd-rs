# SessionParameter native row — 8.3.27.1989

Status: experimental structural evidence. Generated bytes have not been
accepted by a running 1C platform.

The evidence was collected read-only from an existing local `_Config` dump
and its matching XCF export. No 1C process was started and no database row was
written. The bounded `SessionParameter.АвторизованныйПользователь` primary row
had these characteristics:

- logical key: metadata object UUID, without a suffix;
- storage encoding: raw DEFLATE over UTF-8 text with a required UTF-8 BOM;
- compressed bytes: 217;
- plaintext bytes: 349;
- compressed SHA-256:
  `79a15f721d998afa8fb191f7e71082948bd043dc38f365e7d895dde3a1398862`;
- complete redacted grammar:

```text
{1,{1,{2,<metadata-header>,
  {"Pattern",<pattern-item>...}
}},0}

<pattern-item> =
    {"B"}
  | {"S"}
  | {"S",<length>,1}
  | {"N",<digits>,<fraction-digits>,<nonnegative-0-or-1>}
  | {"D"}
  | {"#",<generated-or-built-in-TypeId>}
```

`metadata-header` is the same exact code-3 header documented by the Language
fixture. Read-only corpus checks confirmed that the native pattern preserves
XCF type order and uses `{"S"}` for zero-length variable strings,
`{"S",N,1}` for bounded variable strings, `0/1` for `Any/Nonnegative` number
signs, and `{"D"}` for `DateFractions=DateTime`. Fixed strings and Date-only or
Time-only qualifiers remain fail-closed because their native encodings have
not been evidenced.

The following built-in XCF types were confirmed against existing rows:

| XCF type | Native TypeId |
|---|---|
| `v8:FixedArray` | `4500381b-db30-4a10-9db4-990038032acf` |
| `v8:FixedMap` | `220455ea-6c85-4513-996f-bbe79ed07774` |
| `v8:FixedStructure` | `3ee983d7-ace7-40f9-bb7e-2e916fcddd56` |
| `v8:UUID` | `fc01b5df-97fe-449b-83d4-218a090e681e` |
| `v8:ValueStorage` | `e199ca70-93cf-46ce-a54b-6edc88c3a296` |

The committed layout `session-parameter-v1-crlf-utf8-bom` resolves each
readable `cfg:ObjectGeneratedType.Name` only from the validated canonical
object's `GeneratedType/TypeId`; it never derives or guesses a UUID. Missing
or ambiguous types, duplicate pattern items, unknown tags, non-canonical
numbers and UUIDs, nil TypeIds, unsupported qualifier variants, mismatched
wrapper counts, and extra native fields fail closed. Fixtures cover XCF 2.20
and 2.21 through XML -> canonical IR -> deterministic native blob -> native
IR -> XML -> canonical IR.
