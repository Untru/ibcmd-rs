# Полный диагностический прогон УТ на `3fa316b`

Дата завершения: 26 июля 2026 года.

Этот документ фиксирует воспроизводимую диагностическую точку отсчёта. Он не
подтверждает выпускную готовность и не заменяет строгий gate с полным набором
корневых метаданных и исходных ресурсов.

## Неизменяемый контекст

- Git commit:
  `3fa316b73d461512cfc2291dbce43adaa78bf7a2`.
- Git status: `clean`, `dirty_entries = 0`.
- Бинарный файл `ibcmd-rs`: SHA-256
  `115358274a9833248ba1503312f59bc207de8e1bd16ae11b0be770f3523536f4`.
- Нативный `ibcmd` 8.3.27.1989: SHA-256
  `b71206d1decff7ecfc399e546ec03e6bb3d8eff7b8a5c758496ce663d13d95c2`.
- Версия XML и исходников: `2.20`.
- Режим запуска: полный диагностический, без
  `RequireCompleteRootMetadata` и `RequireCompleteSourceAssets`.

Отпечаток таблиц `Config` и `ConfigSave` до и после прогона одинаков:

```text
rows = 40625
sha256 = a70a53f2ceb6063a7a461fe5515144661b0e2958768919716de318eb442ce20f
unchanged = true
```

## Результат

Все этапы завершились с кодом `0`: нативный экспорт, кандидатный экспорт,
формирование source-layout, raw diff, сигнатурная классификация и матрица.

| Статус | Файлов |
|---|---:|
| Побайтово идентичны | 46 010 |
| Отличаются | 3 538 |
| Только в нативной выгрузке | 75 |
| Только в выгрузке `ibcmd-rs` | 0 |
| Всего в матрице | 49 623 |
| Остаётся до `0 / 0 / 0` | 3 613 |

Текущая диагностическая совместимость:

```text
46010 / 49623 = 92,7200%
```

Нативное дерево:

```text
files = 49623
bytes = 2263598686
sha256 = b0855df0a8413fbe7b401e9e8dedefcaaec4ca5f1fa760f14a08ecb50dc14701
```

Кандидатное дерево:

```text
files = 49548
bytes = 2254219489
sha256 = 1d17e3f627bd70ea102f0a62e5582294603f80c4d0e2fa70e3a24813356231c1
```

## Неполнота

Корневые метаданные:

```text
expected = 13418
emitted = 13343
missing = 75
candidate_set_complete = true
reason = formatter
```

Исходные ресурсы:

```text
expected = 13618
emitted = 12614
missing = 972
opaque = 32
opaque_property_count = 41
candidate_set_complete = false
```

Крупнейшие причины неполноты исходных ресурсов: ошибки decoder прав ролей,
справки форм и семейств метаданных, неопределённый владелец общей формы,
командные интерфейсы и непрозрачные `ChoiceParameters`.

## Критерий завершения

Для закрытия прямого экспорта требуются новые полные прогоны УТ и БСП,
которые одновременно:

1. запускаются на чистом неизменяемом коммите;
2. сохраняют одинаковый отпечаток базы до и после;
3. проходят оба строгих gate;
4. включают `ConfigDumpInfo.xml`;
5. дают `different = 0`, `left_only = 0`, `right_only = 0`.
