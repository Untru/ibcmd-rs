# Доказательство исправления default-формата MXL

Дата проверки: 24 июля 2026 года.

Проверенный коммит:
`e72d5952f7bc974e9dea2f88033cc2c27ecf543d`.

Исправление удаляет ложное правило, по которому само наличие нескольких
наборов колонок создавало document-wide `defaultFormatIndex` и пустой хвостовой
`format`. Настоящий global default и defaults отдельных наборов колонок
сохраняются отдельными доказанными путями.

Нативные деревья повторно использованы из сохранённых неизменяемых прогонов.
Кандидаты УТ и БСП полностью пересобраны release-сборкой с
`platform-oracle`. Это доказывает дельту кандидата, но не заменяет финальный
свежий native-gate.

## Локальная приёмка

- `cargo test spreadsheet_pack_extract --lib`: 13 из 15 тестов прошли;
  исправлены три теста задачи, остались два независимых baseline-падения;
- `cargo test moxel --lib`: 131 из 131;
- `cargo check`: успешно;
- независимый review: ACCEPT, замечаний P1/P2 нет;
- полный `cargo test --lib`: новых падений относительно `master` нет.

## УТ

Каталог артефактов:
`E:\ibcmd_lab\parity\ut_ibcmd_mxl271_e72d595_20260724`.

| Статус | До исправления | После исправления |
|---|---:|---:|
| Неизменённые | 45 863 | 45 864 |
| Изменённые | 3 685 | 3 684 |
| Только в нативной выгрузке | 75 | 75 |
| Только в выгрузке `ibcmd-rs` | 0 | 0 |

Точное совпадение:
`45864 / 49623 = 92,42488362251376%`, в README округлено до **92,4249%**.
До полного совпадения остаётся **3 759 файлов**.

Переходная матрица:

| Переход | Файлов |
|---|---:|
| `different -> unchanged` | 1 |
| `unchanged -> different` | 0 |
| `different -> different` | 3 684 |
| `unchanged -> unchanged` | 45 863 |
| `left_only -> left_only` | 75 |

Побайтово точным стал файл:

- `DataProcessors/ЗаявлениеНаВыпускНовогоКвалифицированногоСертификата/Templates/Сертификат/Ext/Template.xml`.

## БСП

Каталог артефактов:
`E:\ibcmd_lab\parity\bsp_mxl271_e72d595_20260724`.

| Статус | До исправления | После исправления |
|---|---:|---:|
| Неизменённые | 10 546 | 10 547 |
| Изменённые | 1 652 | 1 651 |
| Только слева / только справа | 0 / 0 | 0 / 0 |

Точное совпадение:
`10547 / 12198 = 86,46499426135432%`, в README округлено до **86,4650%**.

Переходная матрица содержит один `different -> unchanged`,
`10546 unchanged -> unchanged` и `1651 different -> different`.
Побайтово точным стал файл:

- `DataProcessors/_ДемоПечатьДоговораКуплиПродажи/Templates/ПФ_MXL_ДоговорКуплиПродажи/Ext/Template.xml`.

## Контрольные суммы

| Артефакт | SHA-256 |
|---|---|
| Release `ibcmd-rs.exe` | `E4F7436B6AD07C867A8DF5158CA17F10B1DB7938178467B8111E877CAC2D1B8A` |
| УТ `raw-diff.json` | `A099F54B0D45AFA458466EB4D5AE6853EFA03E97A35B8C4EBB4AC14FEDBF097D` |
| УТ `matrix.json` | `50414ABF0BBD3FC155BEC8994060597CF62017C759822C7B053057EDE3899A6C` |
| УТ `signatures.json` | `F9CE8400D463C70706215512C6AA3B862B1EF49663980C636D40AB8DD903B143` |
| БСП `raw-diff.json` | `65935B1AE97B0F9361D54678BCCAB31C83468531E40DF7CF95D8E19657EEBCC8` |
| БСП `matrix.json` | `8D8B040737D141F46B46FDD0495A4E95A0E7B4CAEBBD9A4704D2412571F7EFF7` |
| БСП `signatures.json` | `71F5511279B5DEAACF642A3F511B6A21D29E0110033AD8C15B7D074ACCD30055` |

Результат не означает полной совместимости: в УТ остаётся 3 759 файлов, а в
диагностическом сравнении БСП — 1 651 файл.
