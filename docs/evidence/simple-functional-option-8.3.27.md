# FunctionalOption native row — 8.3.27.1989

Status: experimental structural evidence. Generated bytes have not been
accepted by a running 1C platform.

The evidence was collected read-only from an existing local `_Config` dump.
No 1C process was started and no database row was written. The bounded
`FunctionalOption.АктуальнаяДатаОбновленияПовторноИспользуемыхЗначенийМРО`
primary row had these characteristics:

- logical key: metadata object UUID, without a suffix;
- storage encoding: raw DEFLATE over UTF-8 text, without a UTF-8 BOM;
- compressed bytes: 241;
- plaintext bytes: 401;
- compressed SHA-256:
  `ad6cd6fd3855d33a1f4dafd645f8a0a27dc16dc67e776d360dc2c41f2328834d`;
- complete redacted grammar:

```text
{1,{2,<metadata-header>,<location-uuid-or-nil>,
  {0,N,<content-reference>...},<privileged-get-mode-0-or-1>},0}

<content-reference> =
  {"#",3ea29ea5-66f6-4e3b-8595-d8940db766a2,
    {0,{"#",157fa490-4ce9-11d4-9415-008048da11f9,{1,<object-uuid>}}}}
```

`metadata-header` is the same exact code-3 header documented by the Language
fixture. The empty observed `Content` list is `{0,0}`. A second observed row
with non-empty content confirms both nested reference-class UUIDs, the count,
and reference ordering.

The committed layout `functional-option-v1-crlf-no-bom` resolves readable XCF
`Location` and `Content/xr:Object` values only through a validated canonical
graph. Missing or ambiguous references, duplicate targets, unknown wrapper
classes, mismatched counts, non-canonical booleans, and extra native fields
fail closed. Fixtures cover XCF 2.20 and 2.21 through XML -> canonical IR ->
native blob -> native IR -> XML -> canonical IR.
