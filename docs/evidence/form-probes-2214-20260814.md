# Формовые evidence-пробы FE1–FE3 — 8.3.27.2214 / XML 2.20 (2026-08-14)

Девятая лабораторная сессия Parallels «Windows 11» (файловые ИБ, без SQL). Authority:
1С `8.3.27.2214`, `ibcmd.exe` SHA-256
`11c77778927faef858fa4ab544ed627b9b6824a623ee7e5d6e6d5a0cf732d02b`, locale `ru_RU`.
Протокол batch-стандартный; база — retained CF корпуса dcs-form-dynamic-list-server-state;
все принятые пробы — две независимые свежие ИБ, побайтовый детерминизм, первая попытка.

## Новые корпуса

| Корпус | Проба | Формат-факты |
|---|---|---|
| `form-table-representation-matrix` | Одна форма, четыре таблицы (default / List / HierarchicalList / Tree) | Raw-код представления wrapper-55: **default = "1"** (native опускает `<Representation>`; семантика List), **явный List = "0"**, **явный Tree = "2"**. `HierarchicalList` платформа молча дропает при import: native опускает, raw неотличим от default — wire-код не различим, non-claim. Прежний код-маппинг «"0"→List» верен только для явной формы; «"1"» — сентинел «не задано». |
| `form-usual-group-options` | Три вариации UsualGroup (plain / ShowTitle=false / extended) | Все вариации эмитят options-дискриминатор **"29"**; форма **"38" в clean-room 2214 не наблюдается** (легаси реальных дампов). ShowTitle-слот — `options[4]` (1→0). `Group=HorizontalIfPossible` на бездетной группе молча дропается — слот Group не подтверждён и не опровергнут. |

## Отрицательный результат (корпус не создавался)

FE2: гипотеза «таблица на `v8:ValueTable`-атрибуте порождает wrapper-73» опровергнута —
в обеих вариациях (default и с явными EnableStartDrag/EnableDrag) storage остаётся
wrapper `"55"`. Настоящий триггер wrapper-73 не установлен; разрыв двух свойств
wrapper-73 остаётся открытым evidence-вопросом (вероятный источник — реальная база УТ).

## Cleanup

Каталог сессии `C:\lab9` удалён; попутно удалены мусорные каталоги ранних сессий
(`C:\lab-batch$coord$r`, `C:\lab-chips${probe}`). VM остановлена; основной чекаут
не изменялся.
