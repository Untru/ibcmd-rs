#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	СнятьПоказание();
	ПодключитьОбработчикОжидания("СнятьПоказание", 1, Ложь);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

#КонецОбласти

#Область ОбработчикиКомандФормы

#КонецОбласти

#Область ОбработчикиОповещений

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура СнятьПоказание() Экспорт
	Попытка
		Маркер = ОтчетыКлиентПереопределяемый.IbcmdRsМаркерАктивации();
	Исключение
		Маркер = "ERROR: " + ОписаниеОшибки();
	КонецПопытки;

	Путь = "C:\\Users\\Pavel\\ibcmd-rs\\openspec\\changes\\direct-mssql-online-activation\\evidence\\extension-live-observation.txt";
	Запись = Новый ЗаписьТекста(Путь, КодировкаТекста.UTF8);
	Запись.ЗаписатьСтроку(Формат(ТекущаяДата(), "ДФ=yyyy-MM-dd HH:mm:ss") + "|" + Маркер);
	Запись.Закрыть();
КонецПроцедуры

#КонецОбласти
